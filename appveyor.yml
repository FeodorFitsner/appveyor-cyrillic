version: 1.0.{build}
image: Previous Visual Studio 2015
build:
  project: SpecialForAppveyor
  verbosity: minimal
before_build:
- cmd: nuget restore
after_build:
- cmd: nuget pack СпециальноДляAppveyor\СпециальноДляAppveyor.nuspec -Version %APPVEYOR_BUILD_VERSION% -Properties "CONFIGURATION=%CONFIGURATION%"
artifacts:
- path: '*.nupkg' 